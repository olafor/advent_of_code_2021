#include <iostream>
#include <fstream>
#include <vector>
#include <algorithm>
#include <numeric>
#include "utilities/funcprog.h"

int main() {
  std::vector<std::string> numbers{};
  std::ifstream inputFile("BinaryDiagnosticInput.txt");

  std::string num{};
  while (inputFile >> num) {
    numbers.emplace_back(num);
  }

  int numLength = numbers[0].length();
  std::vector<int> summedColumnOnes(numLength, 0);
  int 

  for (auto sumOfOnes : summedColumnOnes) {
    if sum(OfOnes > numbers.length() / 2) {
        
  

}
